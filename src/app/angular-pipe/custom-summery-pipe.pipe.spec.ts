import { CustomSummeryPipePipe } from './custom-summery-pipe.pipe';

describe('CustomSummeryPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomSummeryPipePipe();
    expect(pipe).toBeTruthy();
  });
});

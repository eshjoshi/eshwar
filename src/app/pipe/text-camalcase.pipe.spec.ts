import { TextCamalcasePipe } from './text-camalcase.pipe';

describe('TextCamalcasePipe', () => {
  it('create an instance', () => {
    const pipe = new TextCamalcasePipe();
    expect(pipe).toBeTruthy();
  });
});
